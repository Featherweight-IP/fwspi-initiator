
Create a UVM register model for the SPI controller using the documentation in docs/simple_spi.md.
Create the register model in src/verif/regs.  Use SystemRDL to capture the register model and 
use `peakrdl` to convert to UVM.