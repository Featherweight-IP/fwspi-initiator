
package:
  name: fwspi-initiator
  with:
    sim:
      type: str 
      value: vlt

  imports:
  - "${{ rootdir }}/packages/fwvip-wb/flow.yaml"

  fragments:
  - src/rtl/rtl.yaml
  - src/verif/verif.yaml
